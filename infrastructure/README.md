# Infrastructure

Development infrastructure is defined in the root `docker-compose.yml`. Production manifests, telemetry collectors, backups, and secret-vault integration are intentionally deferred until their roadmap milestones.

